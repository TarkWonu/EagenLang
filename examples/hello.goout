시작!
GO척결.출력("안녕하세요!")
GO척결.변수 와샌즈 = "장한울 특검 메시지: "
GO척결.출력(와샌즈 + 123 + "번")

GO척결.함수 인사(대상) {
  GO척결.출력("GO 언어는 " + 대상)
}
GO척결.호출(인사, "죄악이다")
GO척결.출력("그리고 백엔드는 재앙이다.")
GO척결.출력("이 둘이 만나면, 삶은 지옥이 된다.")

GO척결.반복i(0, 3) {
  GO척결.출력("i=" + i)
}
GO척결.변수 i = 4
GO척결.만약(i < 3 && !(i == 1)) {
  GO척결.출력("세 번 미만 & 1은 아님")
} GO척결.디떨이 아니다?! {
  GO척결.출력("세 번 이상 또는 1")
}
장한울을 혁명적으로 특검해야 한다
