시작!
GO척결.출력("Hello World WithOut GO")
GO척결.변수 와샌즈 = "장한울 특검 메시지: "
GO척결.출력(와샌즈 + 123 + "번")

GO척결.함수 특검(대상) {
  GO척결.출력(대상+"을 혁명적으로 특검!")
}
GO척결.출력("Go언어는 죄악이다")
GO척결.출력("그리고 백엔드는 재앙이다.")
GO척결.출력("이 둘이 만나면, 삶은 지옥이 된다.")

GO척결.호출(특검,"장한울")

GO척결.반복i(1, 4) {
  GO척결.출력("장한울을 " + i + "번 특검")
}
GO척결.변수 주로쓰는언어 = "Go"
GO척결.만약(주로쓰는언어 == "GO"||주로쓰는언어 == "Go"){
  GO척결.출력("특검 실시")
}GO척결.디떨이 아니다?!{
  GO척결.출력("너는 살려준다 ㅇㅇ")
}



장한울을 혁명적으로 특검해야 한다
